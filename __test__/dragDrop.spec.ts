import '@testing-library/jest-dom/extend-expect';
import 'jest';
import { render, cleanup } from '@testing-library/svelte';
import DragDrop from '../dragDrop.svelte';

describe('receive items dragDrop component', () => {
    it('', () => {
        
    });
    });
    afterEach(() => {
        cleanup();
    });
